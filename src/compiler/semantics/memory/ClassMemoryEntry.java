package compiler.semantics.memory;

public class ClassMemoryEntry extends MemoryEntry {
    public ClassMemoryEntry(String name, int lineNumber, String scope) {
        super(name, lineNumber, scope);
    }
}
